# AV Manager

![示例图片](public/ex.png)

一个运行在本地、管理私人影片库的 Next.js 应用：自动扫描本地视频文件，抓取封面与元数据，提供搜索筛选、播放进度记忆、Elo 对战评分和重复影片整理。

## 功能

- **影库扫描**：扫描指定目录下的视频文件，按番号自动抓取封面、标题、演员、类型等元数据
- **浏览与检索**：按时间 / 评分 / 文件大小排序；支持按标题、番号、演员、文件名搜索；演员与类型标签点选即筛
- **播放**：内置视频播放器（流式播放），记忆播放进度、播完状态与观看次数
- **影片对战**：随机抽取两部影片对比打分，基于 Elo 评分（含不确定度 sigma、新片快速收敛、平局记录），支持悔棋
- **重复整理**：识别同番号多版本文件，分组展示便于清理
- **删除**：卡片或播放器内二次确认后直接删除磁盘文件

## 技术栈

- Next.js (App Router) + React + TypeScript
- Tailwind CSS
- node:sqlite 本地数据库（影片元数据、评分、播放历史）
- Playwright 用于元数据抓取

## 使用方法

1. 克隆仓库：
   ```bash
   git clone https://github.com/1998SUIYUE/av_manager_nextjs.git
   ```
2. 安装依赖：
   ```bash
   npm install
   ```
3. 启动开发服务器：
   ```bash
   npm run dev
   ```
4. 打开浏览器访问 [http://localhost:3000](http://localhost:3000)

## 注意事项

- 首次打开需要扫描并抓取元数据，视影片数量耐心等待
- 数据库文件生成在 `userData/` 目录下，包含所有缓存数据；删掉它即可完全重置
- 本项目仅用于本地环境，未做任何鉴权，请勿暴露到公网
